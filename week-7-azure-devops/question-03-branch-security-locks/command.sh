# (Optional) Using Azure CLI to manage repo permissions
# Note: Actual permission locking often requires UI or REST API

# List repos in the project
az repos list --project "YourProjectName"

# (UI Alternative) Use REST API to set permissions if needed
