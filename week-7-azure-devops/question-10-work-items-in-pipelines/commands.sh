# No actual CLI for linking work items, but here are Git examples

# Commit with work item ID
git add .
git commit -m "Add database rollback logic #108"
git push origin main

# You can also manually link work items via pull request UI in Azure DevOps
