
---

### 📄 6. `notes.md`

```markdown
## Notes – Persistent Volume Claim in AKS

- PVCs allow dynamic or static allocation of storage in Kubernetes.
- The pod uses PVC to mount storage and retain data across restarts.
- Useful for stateful applications (e.g., databases, CMS).
- StorageClass can be changed for cloud-managed storage on Azure.
